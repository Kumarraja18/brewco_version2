# Coffee Cloud — Frontend (React)

Minimal React-only frontend scaffold (Vite).

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173

Next steps: add API client, auth flows, and components for cafés, booking, menu, and payment UI.
