# ================================================================
# Brew & Co — Environment Variables
# ================================================================
# Copy this file to .env and fill in YOUR values:
#   copy .env.example .env
#
# The app will work even WITHOUT this file using smart defaults:
#   DB_USERNAME=root, DB_PASSWORD=(empty), DB_NAME=brewco_db
# ================================================================

# Database (MySQL)
# DB_NAME    → database name (auto-created if missing)
# DB_USERNAME → your MySQL user (default: root)
# DB_PASSWORD → your MySQL password (default: empty)
DB_NAME=brewco_db
DB_USERNAME=root
DB_PASSWORD=your_mysql_password_here

# Admin Account (created automatically on first startup)
ADMIN_EMAIL=admin@brewco.com
ADMIN_PASSWORD=admin123

# Email (Gmail SMTP) — OPTIONAL
# The app works fine without email. Approval passwords are
# logged to console instead of being emailed.
# To enable email:
#   1. Go to https://myaccount.google.com/apppasswords
#   2. Generate an app password for "Mail"
#   3. Paste it below
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_gmail_app_password_here

# JWT Configuration
JWT_SECRET=your-minimum-32-character-random-secret-string-here

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Razorpay Configuration
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
